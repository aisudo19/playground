#include<iostream>
#include<string>
#include<algorithm>
#include<stdio.h>
#include<queue>
#include<stack>
#include<map>

using namespace std;

typedef long long ll;
typedef double db;
typedef vector<int> vint;
typedef vector<ll> vll;
typedef pair<int, int>P;

int N,R;
int X[MAX_N];

void solve() {
    sort(X, X + M);

    int i = 0, ans = 0;
    while (i<N){
        int s=x[i++]
        while (i<N && X[i]<=s+R)i++;

        int p=X[i-1];
        while(i<N && X[i])
    }

}