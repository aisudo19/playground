#include<iostream>
#include<string>
#include<algorithm>
#include<stdio.h>
#include<queue>
#include<stack>

using namespace std;

typedef long long ll;
typedef double db;
typedef vector<int> vint;
typedef vector<ll> vll;

int main(){
    stack<int> intStack;
    stack<string> strStack;

    intStack.push(6);
    strStack.push("hello");
    str.Stack.push("world");

    intStack.top();//6
    cout<<strStack.top()<<endl;//world

    intStack.pop();

    strStack.size();
    strStack.empty();

    return 0;
 }